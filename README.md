# Tetris

## Tetris in the terminal made in python

## Install guide

1. Clone: `git clone https://github.com/patpat1004/tetris.git && cd tetris`
2. Install dependencies: `pip install -r requirements.txt`
